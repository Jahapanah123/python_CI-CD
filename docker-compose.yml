version: '3.8'

services:
  db:
    image: postgres:15
    container_name: todo_postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=todo_db
    ports:
      - "5433:5432"